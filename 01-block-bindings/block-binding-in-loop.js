/* Block Binding in Loops */
'use strict';

const myArr = ['cat', 'dog', 'elephant', 'cammel'];

for (let i = 0; i < myArr.length; i++) {
  console.log(myArr[i]);
};

//console.log(i); // I value it's not accessible here, it will throw an error